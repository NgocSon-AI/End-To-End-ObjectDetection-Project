from setuptools import find_packages, setup

setup(
    name = 'signLanguages',
    version='0.0.0',
    author='SunAI',
    author_email='286sunsun@gmail.com',
    packages=find_packages(),
    install_requires=[],
)

